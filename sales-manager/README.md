# מנהל מכירות (Sales Manager)

אפליקציית React לניהול מכירות, כרטיסיות נושאים, מעקב קופה ומחירון מוצרים. הנתונים נשמרים אוטומטית ב-localStorage של הדפדפן.

## הרצה מקומית

```bash
npm install
npm run dev
```

## בנייה לפרודקשן

```bash
npm run build
```
